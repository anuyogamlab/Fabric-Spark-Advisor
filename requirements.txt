# Core dependencies (compatible versions - all tested together)
# Updated to resolve httpx and starlette conflicts
fastmcp==0.2.0
semantic-kernel==1.1.0
azure-kusto-data==4.4.0
azure-search-documents==11.4.0
azure-identity==1.15.0
openai==1.12.0
chainlit>=1.3.0
python-dotenv==1.0.0
uvicorn>=0.27.0
# Let chainlit specify starlette and sse-starlette versions
# Removed pinned starlette and sse-starlette to allow pip to resolve
