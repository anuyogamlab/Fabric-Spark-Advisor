# Core dependencies — pinned for compatibility
# Validated chain: SK 1.40 + openai 2.x + pydantic 2.9.x + mcp 1.26
# mcp 1.26.0 requires pydantic>=2.11; spark_mcp_server.py patches _create_wrapped_model
# to fix the pydantic 2.10+ breaking change in create_model(name, result=type) syntax.
fastmcp==0.2.0
mcp>=1.3.0
semantic-kernel==1.40.0    # 1.40+ supports openai 2.x; requires pydantic<2.12
openai>=2.0,<3.0           # SK 1.40 requires openai>=2.0
pydantic>=2.11,<2.12       # mcp 1.26 requires >=2.11; SK 1.40 requires <2.12
azure-kusto-data==4.4.0
azure-search-documents==11.4.0
azure-identity>=1.15.0
starlette>=0.27.0
httpx>=0.25.0
python-dotenv==1.0.0
uvicorn>=0.27.0
